🤔 console.log([]==[])
    console.log([]===[])

🅰️ Primitives → value comparison
    5 === 5        // true
    "hi" === "hi"  // true


    Objects/Arrays → reference compariso
    Arrays are non-primitive (objects)
    👉 Objects are compared by reference, not by value

    [] === []
    === checks same type AND same reference
    Each [] creates a new array in memory
    They live at different memory locations

    [] == []
    == allows type coercion only for primitives
    Objects are never coerced into other objects
    JavaScript still compares references

    {} === {}     // false
    [] === []     // false
    () => {} === () => {} // false

    const a = [];
    const b = a;

    console.log(a === b); // true
    console.log(a == b);  // true
-----------------------------------------------------------------------------------------------------------------------------------------
🤔 Arrow function can be used as a contructor ? If new keyword is used ?

-----------------------------------------------------------------------------------------------------------------------------------------
🤔 var a = 50
    {
        var a = 5000
    }
    let b = a
    {
    let b = 50000
    }
    console.log(b)   // 5000

    var a = 50
    function sample1(){
        var a = 5000
    }
    let b = a
    function sample2(){
    let b = 50000
    }
    console.log(b)  // 50
-----------------------------------------------------------------------------------------------------------------------------------------
🤔 console.log(2-"2")   // 0

    console.log("2"-"2")   // 0

    console.log("abhi"-"2")   // NaN

-----------------------------------------------------------------------------------------------------------------------------------------
🤔  const arr = [2,2,1,1,4,5,6,8,8,3,9,10]  // find the duplicate elements from the array

output should [2,1,8]
-----------------------------------------------------------------------------------------------------------------------------------------
🤔 falsy values available in JS ?

-----------------------------------------------------------------------------------------------------------------------------------------
🤔 Async vs Defer

-----------------------------------------------------------------------------------------------------------------------------------------
🤔 const str = "prashant"  (find 1st repeated character in the string)

-----------------------------------------------------------------------------------------------------------------------------------------
🤔 const arr = [1,2,3,4,5]

    const newArr = arr.map((item) => item === 2)
    const newArr = arr.map((item) => item == 2)

    console.log(newArr)
-----------------------------------------------------------------------------------------------------------------------------------------
🤔 console.log(10 + "8" + 20) , console.log(10 - "8" - 20)

-----------------------------------------------------------------------------------------------------------------------------------------
🤔 let abc = 100;

if(function xyz(){} ){
    abc = abc - typeof(xyz)
}

console.log(abc)   // NaN
------------------------------------
let abc = 100;

if(function xyz(){} ){
    abc = abc + typeof(xyz)
}

console.log(abc)   // 100undefined
-----------------------------------------------------------------------------------------------------------------------------------------
🤔 const obj = {
    name : "prabitra",
    xyz : function (){
        console.log(this.name)
    }
}

const c = obj.xyz
obj.xyz()
console.log(c)
c()
-----------------------------------------------------------------------------------------------------------------------------------------
🤔 const str1 = "pk"
    const str2 = "prashant"
    const str3 = str1 || str2
    const str3 = str1 && str2
    console.log(str3)

-----------------------------------------------------------------------------------------------------------------------------------------
🤔  let a = 100
    // const b = a++ + ++a
    const b = ++a + a++
    console.log(b)
-----------------------------------------------------------------------------------------------------------------------------------------